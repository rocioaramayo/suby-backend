spring:
  application:
    name: suby

  datasource:
    url: jdbc:mysql://localhost:3306/suby
    username: root
    password: R46742481######
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: 8080


application:
  security:
    jwt:
      secretKey: suby_super_secret_key_suby_super_secret_key_2026
      expiration: 86400000